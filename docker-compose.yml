services:
  fastapi-app:
    build: .
    container_name: app-flask-raphael
    ports:
      - "3001:3000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    command: uvicorn main:app --host 0.0.0.0 --port 3000 --reload
